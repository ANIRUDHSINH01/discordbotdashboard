{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",    // Entry point inside the src directory
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.js"     // Routing all requests to src/index.js
    }
  ]
}
